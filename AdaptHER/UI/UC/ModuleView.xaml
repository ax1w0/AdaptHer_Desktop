<UserControl x:Class="AdaptHER.UI.UC.ModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdaptHER.UI.UC"
             mc:Ignorable="d" 
                         d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border x:Name="header" Grid.Row="0" CornerRadius="8" Background="White">
            <Grid>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="2.5*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="OrgModuleName" Grid.Column="0" Margin="10 " FontWeight="Bold" Foreground="{DynamicResource FourthIconColor}" FontSize="16" HorizontalAlignment="Stretch">
                    <Run Text="Organizational Module Number"/>
                    </TextBlock>
                    <TextBlock x:Name="moduleName" Grid.Column="1" Margin="10" FontWeight="Bold" Foreground="{DynamicResource FourthIconColor}" FontSize="16" HorizontalAlignment="Stretch">
                    <Run Text="Module Name"/>
                    </TextBlock>
                </Grid>

                <RadioButton x:Name="collapsedButton" Width="15" HorizontalAlignment="Right" Margin="12" IsEnabled="False" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="0 15 0 0" CornerRadius="8" Background="White" x:Name="body">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="15 10 15 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" HorizontalAlignment="Right" Orientation="Horizontal">
                        <Button Grid.Column="0" Height="25" Width="25" Margin="15 0" HorizontalAlignment="Right" x:Name="EditButton" Click="EditButton_Click">
                            <Image Source="/Resources/UpdateBtn.png" Width="14" Height="14"/>
                        </Button>
                        <Button Grid.Column="0" Height="25" Width="25" HorizontalAlignment="Right" x:Name="SaveButton" IsEnabled="False" Click="SaveButton_Click">
                            <Image Source="/Resources/SaveBtn.png" Width="14" Height="14"/>
                        </Button>
                    </StackPanel>
                    <TextBlock Grid.Column="1" Text="Status:" Margin="15 0" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>
                    <ComboBox Grid.Column="2" IsEnabled="False" x:Name="moduleStatus" Height="25" IsEditable="False"/>
                </Grid>

                <Grid Grid.Row="1">
                    <TextBlock Margin="20 0">Developers:</TextBlock>
                    <ListBox x:Name="developers" Margin="15 20 15 5" ScrollViewer.VerticalScrollBarVisibility="Visible" SelectionMode="Multiple" MaxHeight="80">
                        <ListBox.Template>
                            <ControlTemplate TargetType="{x:Type ListBox}">
                                <Border CornerRadius="8" BorderThickness="2" BorderBrush="{DynamicResource PrimaryHomingColor}" Background="White">
                                    <ScrollViewer Margin="5">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </ControlTemplate>
                        </ListBox.Template>
                    </ListBox>
                </Grid>

                <Grid Grid.Row="2">
                    <TextBlock Margin="20 0">Coordinated:</TextBlock>
                    <ListBox x:Name="agreed" Margin="15 20 15 5" ScrollViewer.VerticalScrollBarVisibility="Visible" SelectionMode="Multiple" MaxHeight="80">
                        <ListBox.Template>
                            <ControlTemplate TargetType="{x:Type ListBox}">
                                <Border CornerRadius="8" BorderThickness="2" BorderBrush="{DynamicResource PrimaryHomingColor}" Background="White">
                                    <ScrollViewer Margin="5">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </ControlTemplate>
                        </ListBox.Template>
                    </ListBox>
                </Grid>

                <Grid Grid.Row="3">
                    <TextBlock Margin="20 0">Positions:</TextBlock>
                    <ListBox x:Name="roles" Margin="15 20 15 15" ScrollViewer.VerticalScrollBarVisibility="Visible" SelectionMode="Multiple" MaxHeight="80">
                        <ListBox.Template>
                            <ControlTemplate TargetType="{x:Type ListBox}">
                                <Border CornerRadius="8" BorderThickness="2" BorderBrush="{DynamicResource PrimaryHomingColor}" Background="White">
                                    <ScrollViewer Margin="5">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </ControlTemplate>
                        </ListBox.Template>
                    </ListBox>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
